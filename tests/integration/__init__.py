"""Integration tests for IGRIS_GPT (#1320).

These tests require real LLM providers (Ollama, cloud APIs) and are gated
behind the IGRIS_INTEGRATION_TESTS=1 environment variable. They do not
run in normal CI.
"""
