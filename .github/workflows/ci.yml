name: CI

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install -U pip
          python -m pip install -e ".[dev]"
      - name: Run tests
        env:
          IGRIS_REQUIRE_AUTH: "true"
          IGRIS_PROJECT_ROOT: "/tmp/igris_ci_test"
        run: python -m pytest -q

  type-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: |
          python -m pip install -U pip
          python -m pip install -e ".[dev]"
      - name: Run pyright (basic mode)
        # Phase 2 (#1355): || true kept until 0 errors in basic mode.
        # Once all type errors are fixed, remove || true to make CI blocking.
        run: python -m pyright igris/ --outputjson || true
