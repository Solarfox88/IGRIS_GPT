"""
Top-level package for the IGRIS_GPT project.

This file makes the project root importable as a Python package so that
tests can import the `igris` package without having to install it via
pip.  It does not define any symbols itself.
"""
